import { theme as primary } from './theme-primary';
import { theme as secondary } from './theme-secondary';
import { theme as tertiary } from './theme-tertiary';
import { theme as neutral } from './theme-neutral';
import { theme as info } from './theme-info';
import { theme as success } from './theme-success';
import { theme as warning } from './theme-warning';
import { theme as danger } from './theme-danger';

export const theme = {
  primary,
  secondary,
  tertiary,
  neutral,
  info,
  success,
  warning,
  danger,
};
